package net.mysticcreations.lib.worldmixer;

public class WorldMixer {
    //Equivalent to Terrablender, but our own thing
    //No code stealin' because licenses, so we'll make it from scratch
    //This also defo is not gonna be the only file
}
